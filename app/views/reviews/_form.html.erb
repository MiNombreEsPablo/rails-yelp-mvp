<%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.input :restaurant_id, :as => :hidden, :input_html => { :value => params[:restaurant_id] } %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit %>
<% end %>
